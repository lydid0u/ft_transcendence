/**
 * This module has been kept for compatibility with the existing code but all VHS effects
 * have been removed as part of the new design.
 */

// Export an empty function to make this a valid module
export function initVHSEffects() {
  console.log('VHS effects have been removed as part of the new design');
  return;
}

// Re-export for compatibility with existing code
export default { initVHSEffects };